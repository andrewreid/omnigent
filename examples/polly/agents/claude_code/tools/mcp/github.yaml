name: github
transport: http
url: https://api.githubcopilot.com/mcp/
headers:
  Authorization: Bearer ${GITHUB_PAT}
description: GitHub MCP server (remote, HTTP transport). Exposes all GitHub MCP tools.
